[package]
name = "deltae"
version = "0.1.0"
edition = "2021"
authors = ["adavis352"]
description = "Command line tool that measures the perceptual difference between two colors"
license = "MIT"

[[bin]]
name = "deltae"
path = "src/main.rs"

[dependencies]
